import { createSlice } from "@reduxjs/toolkit";


const initialState = "Sin definir todavia";

const urlAPISlice = createSlice({
  name: "urlAPI",
  initialState,
  reducers: {},
});

export default urlAPISlice.reducer;
